// modules are defined as an array
// [ module function, map of requires ]
//
// map of requires is short require name -> numeric require
//
// anything defined in a previous bundle is accessed via the
// orig method which is the require for previous bundles
parcelRequire = (function (modules, cache, entry, globalName) {
  // Save the require from previous bundle to this closure if any
  var previousRequire = typeof parcelRequire === 'function' && parcelRequire;
  var nodeRequire = typeof require === 'function' && require;

  function newRequire(name, jumped) {
    if (!cache[name]) {
      if (!modules[name]) {
        // if we cannot find the module within our internal map or
        // cache jump to the current global require ie. the last bundle
        // that was added to the page.
        var currentRequire = typeof parcelRequire === 'function' && parcelRequire;
        if (!jumped && currentRequire) {
          return currentRequire(name, true);
        }

        // If there are other bundles on this page the require from the
        // previous one is saved to 'previousRequire'. Repeat this as
        // many times as there are bundles until the module is found or
        // we exhaust the require chain.
        if (previousRequire) {
          return previousRequire(name, true);
        }

        // Try the node require function if it exists.
        if (nodeRequire && typeof name === 'string') {
          return nodeRequire(name);
        }

        var err = new Error('Cannot find module \'' + name + '\'');
        err.code = 'MODULE_NOT_FOUND';
        throw err;
      }

      localRequire.resolve = resolve;
      localRequire.cache = {};

      var module = cache[name] = new newRequire.Module(name);

      modules[name][0].call(module.exports, localRequire, module, module.exports, this);
    }

    return cache[name].exports;

    function localRequire(x){
      return newRequire(localRequire.resolve(x));
    }

    function resolve(x){
      return modules[name][1][x] || x;
    }
  }

  function Module(moduleName) {
    this.id = moduleName;
    this.bundle = newRequire;
    this.exports = {};
  }

  newRequire.isParcelRequire = true;
  newRequire.Module = Module;
  newRequire.modules = modules;
  newRequire.cache = cache;
  newRequire.parent = previousRequire;
  newRequire.register = function (id, exports) {
    modules[id] = [function (require, module) {
      module.exports = exports;
    }, {}];
  };

  var error;
  for (var i = 0; i < entry.length; i++) {
    try {
      newRequire(entry[i]);
    } catch (e) {
      // Save first error but execute all entries
      if (!error) {
        error = e;
      }
    }
  }

  if (entry.length) {
    // Expose entry point to Node, AMD or browser globals
    // Based on https://github.com/ForbesLindesay/umd/blob/master/template.js
    var mainExports = newRequire(entry[entry.length - 1]);

    // CommonJS
    if (typeof exports === "object" && typeof module !== "undefined") {
      module.exports = mainExports;

    // RequireJS
    } else if (typeof define === "function" && define.amd) {
     define(function () {
       return mainExports;
     });

    // <script>
    } else if (globalName) {
      this[globalName] = mainExports;
    }
  }

  // Override the current require with this new one
  parcelRequire = newRequire;

  if (error) {
    // throw error from earlier, _after updating parcelRequire_
    throw error;
  }

  return newRequire;
})({"../../node_modules/parcel/src/builtins/bundle-url.js":[function(require,module,exports) {
var bundleURL = null;

function getBundleURLCached() {
  if (!bundleURL) {
    bundleURL = getBundleURL();
  }

  return bundleURL;
}

function getBundleURL() {
  // Attempt to find the URL of the current script and use that as the base URL
  try {
    throw new Error();
  } catch (err) {
    var matches = ('' + err.stack).match(/(https?|file|ftp|chrome-extension|moz-extension):\/\/[^)\n]+/g);

    if (matches) {
      return getBaseURL(matches[0]);
    }
  }

  return '/';
}

function getBaseURL(url) {
  return ('' + url).replace(/^((?:https?|file|ftp|chrome-extension|moz-extension):\/\/.+)\/[^/]+$/, '$1') + '/';
}

exports.getBundleURL = getBundleURLCached;
exports.getBaseURL = getBaseURL;
},{}],"../../node_modules/parcel/src/builtins/css-loader.js":[function(require,module,exports) {
var bundle = require('./bundle-url');

function updateLink(link) {
  var newLink = link.cloneNode();

  newLink.onload = function () {
    link.remove();
  };

  newLink.href = link.href.split('?')[0] + '?' + Date.now();
  link.parentNode.insertBefore(newLink, link.nextSibling);
}

var cssTimeout = null;

function reloadCSS() {
  if (cssTimeout) {
    return;
  }

  cssTimeout = setTimeout(function () {
    var links = document.querySelectorAll('link[rel="stylesheet"]');

    for (var i = 0; i < links.length; i++) {
      if (bundle.getBaseURL(links[i].href) === bundle.getBundleURL()) {
        updateLink(links[i]);
      }
    }

    cssTimeout = null;
  }, 50);
}

module.exports = reloadCSS;
},{"./bundle-url":"../../node_modules/parcel/src/builtins/bundle-url.js"}],"scss/index.scss":[function(require,module,exports) {
var reloadCSS = require('_css_loader');

module.hot.dispose(reloadCSS);
module.hot.accept(reloadCSS);
module.exports = {
  "fadeIn": "fadeIn",
  "fadeOut": "fadeOut",
  "primary": "primary",
  "secondary": "secondary",
  "default": "default",
  "rheroicons": "rheroicons",
  "rheroicons_logout": "rheroicons_logout",
  "icon-only": "icon-only",
  "icon": "icon",
  "navigation-wrapper": "navigation-wrapper",
  "navigation-button-list": "navigation-button-list",
  "single-button-list": "single-button-list",
  "navigation-item": "navigation-item",
  "multi-button-list": "multi-button-list",
  "navbar": "navbar",
  "menu": "menu",
  "menu-item": "menu-item",
  "item-hidden": "item-hidden",
  "menu-button": "menu-button",
  "menu-button-label": "menu-button-label",
  "menu-button-icon": "menu-button-icon",
  "item-brand-link": "item-brand-link",
  "menu-link": "menu-link",
  "logo": "logo",
  "item-restart-app": "item-restart-app",
  "item-signout-app": "item-signout-app",
  "main": "main",
  "page": "page",
  "rheroicons_outline": "rheroicons_outline",
  "error-box": "error-box",
  "error-box-text": "error-box-text",
  "error-text": "error-text",
  "signin": "signin",
  "form": "form",
  "form-control": "form-control",
  "page-extra-top-spacing": "page-extra-top-spacing",
  "card-group": "card-group",
  "card": "card",
  "medication-cards": "medication-cards",
  "card-label": "card-label",
  "rheroicons_check": "rheroicons_check",
  "rheroicons_exclamation": "rheroicons_exclamation",
  "progressbar__container": "progressbar__container",
  "progressbar__bar": "progressbar__bar",
  "settings": "settings",
  "accordion__heading": "accordion__heading",
  "accordion__toggle": "accordion__toggle",
  "toggle__label": "toggle__label",
  "toggle__icon": "toggle__icon",
  "accordion__content": "accordion__content",
  "accordion": "accordion",
  "accordion__input": "accordion__input",
  "side-effect-accordion": "side-effect-accordion",
  "accordion__checked": "accordion__checked"
};
},{"_css_loader":"../../node_modules/parcel/src/builtins/css-loader.js"}],"js/_form_validation.js":[function(require,module,exports) {
////////////////////////////////////////////////////////////////////////////////
// FILE: _form_validation.js
// AUTHOR: David Ruvolo
// CREATED: 2020-07-09
// MODIFIED: 2020-07-22
// PURPOSE: Form Validation for Sign in page
// DEPENDENCIES: NA
// STATUS: in.progress
// COMMENTS: NA
////////////////////////////////////////////////////////////////////////////////
// Reset Form
// Define a function that clears form inputs and resets invalid input elements
// @param elem the ID of the form to reset
function reset_login_form(elem) {
  // find form
  var form = document.getElementById(elem); // reset inputs

  var inputs = form.querySelectorAll("input[aria-invalid='true']");

  if (inputs.length) {
    inputs.forEach(function (el) {
      return el.removeAttribute("aria-invalid");
    });
  }
} // showError
// Define a function that shows an input element and error message
// @param elem an ID of the input to make invalid
// @param error an error message to display


function show_login_error(elem, error) {
  // find input and add invalid attribute
  var input = document.getElementById(elem);
  input.setAttribute("aria-invalid", "true"); // extract error element ID and insert text

  var errorID = input.getAttribute("aria-describedby");
  var errorElem = document.getElementById(errorID);
  errorElem.innerText = error; // set listener for blur and not empty

  input.addEventListener("keypress", function _keyup(e) {
    if (e.target.value.length > 2) {
      input.removeAttribute("aria-invalid");
      errorElem.innerText = "";
      input.removeEventListener("keypress", _keyup, true);
    }
  }, true);
} // export


module.exports = {
  reset_login_form: reset_login_form,
  show_login_error: show_login_error
};
},{}],"js/_progress.js":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

////////////////////////////////////////////////////////////////////////////////
// FILE: _progress.js
// AUTHOR: David Ruvolo
// CREATED: 2020-07-09
// MODIFIED: 2020-08-03
// PURPOSE: methods for updating progress bar
// DEPENDENCIES: Shiny
// STATUS: working
// COMMENTS: NA
////////////////////////////////////////////////////////////////////////////////
function progress_data(el, now, max) {
  var elem = document.getElementById(el);
  var container = elem.parentElement;
  var width = container.getBoundingClientRect().width;
  var bins = width / max;
  var rate = bins / width;
  var transform_value = rate * now;
  return {
    width: width,
    rate: rate.toFixed(2),
    transform_value: transform_value
  };
} // update progress bar
// @param el unique ID of progress bar element
// @param now current page number
// @param max max page numbers


function update_progress_bar(el, now, max) {
  var elem = document.getElementById(el);
  var bar = elem.querySelector(".progressbar__bar");
  var d = progress_data(el, now, max);
  bar.style.transform = "scaleX(".concat(d.transform_value, ")");
  bar.style.transformOrigin = "".concat(d.rate, " center");
  elem.setAttribute("aria-valuenow", now);
  elem.setAttribute("aria-valuetext", "page ".concat(now, " of ").concat(max));
} // export


var _default = update_progress_bar;
exports.default = _default;
},{}],"js/_errors.js":[function(require,module,exports) {
////////////////////////////////////////////////////////////////////////////////
// FILE: _errors.js
// AUTHOR: David Ruvolo
// CREATED: 2020-07-21
// MODIFIED: 2020-07-22
// PURPOSE: methods for showing and hiding error messages
// DEPENDENCIES: NA
// STATUS: in.progress
// COMMENTS: NA
////////////////////////////////////////////////////////////////////////////////
// update_error_box
// Show and set the inner text of an error box
// @param id ID of the element
// @param error message to display
function update_error_box(id, error) {
  var elem = document.getElementById(id);
  var text = elem.getElementsByTagName("span")[0];
  elem.style.display = "flex";
  text.innerText = error;
} // reset_error_box
// Hide and clear error box
// @param id id of the error box


function reset_error_box(id) {
  var elem = document.getElementById(id);
  var text = elem.getElementsByTagName("span")[0];
  elem.style.display = "none";
  text.innerText = "";
} // update_error_text
// Show and hide an error text element
// @param id id of the error element
// @param error message to display


function update_error_text(id, error) {
  var elem = document.getElementById(id);
  elem.style.display = "flex";
  elem.innerText = error;
} // reset_error_text
// Hide and reset an error text element
// @param id id of the error element


function reset_error_text(id) {
  var elem = document.getElementById(id);
  elem.style.display = "none";
  elem.innerText = "";
} // export


module.exports = {
  update_error_box: update_error_box,
  update_error_text: update_error_text,
  reset_error_box: reset_error_box,
  reset_error_text: reset_error_text
};
},{}],"index.js":[function(require,module,exports) {
"use strict";

require("./scss/index.scss");

var _form_validation = require("./js/_form_validation");

var _progress = _interopRequireDefault(require("./js/_progress"));

var _errors = require("./js/_errors");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

////////////////////////////////////////////////////////////////////////////////
// FILE: index.js
// AUTHOR: David Ruvolo
// CREATED: 2019-11-11
// MODIFIED: 2020-07-22
// PURPOSE: main js file for app
// DEPENDENCIES: NA
// STATUS: working
// COMMENTS: NA
////////////////////////////////////////////////////////////////////////////////
// import scss
// import js
////////////////////////////////////////
// DOMContentLoaded event: execute only once
window.addEventListener("DOMContentLoaded", function (e) {
  // update html attributes
  var html = document.getElementsByTagName("html")[0];
  html.lang = "en";
  html.dir = "ltr";
}, {
  once: true
}); ////////////////////////////////////////
// bind: reset_login_form

Shiny.addCustomMessageHandler("reset_login_form", function (data) {
  (0, _form_validation.reset_login_form)(data.elem);
}); // bind: show_login_error

Shiny.addCustomMessageHandler("show_login_error", function (data) {
  (0, _form_validation.show_login_error)(data.elem, data.error);
}); // bind: update_error_box

Shiny.addCustomMessageHandler("update_error_box", function (data) {
  (0, _errors.update_error_box)(data.id, data.error);
}); // bind: reset_error_box

Shiny.addCustomMessageHandler("reset_error_box", function (data) {
  (0, _errors.reset_error_box)(data.id);
}); // bind: update_error_text

Shiny.addCustomMessageHandler("update_error_text", function (data) {
  (0, _errors.update_error_text)(data.id, data.error);
}); // bind: reset_error_text

Shiny.addCustomMessageHandler("reset_error_text", function (data) {
  (0, _errors.reset_error_text)(data.id);
}); // bind: update_progress_bar

Shiny.addCustomMessageHandler("update_progress_bar", function (data) {
  setTimeout(function () {
    (0, _progress.default)(data.elem, data.now, data.max);
  }, 200);
}); // bind: fade_page

Shiny.addCustomMessageHandler("fade_page", function (data) {
  var p = document.querySelector(".page");
  p.classList.add("fadeOut");
  p.classList.remove("fadeIn");
});
},{"./scss/index.scss":"scss/index.scss","./js/_form_validation":"js/_form_validation.js","./js/_progress":"js/_progress.js","./js/_errors":"js/_errors.js"}],"../../node_modules/parcel/src/builtins/hmr-runtime.js":[function(require,module,exports) {
var global = arguments[3];
var OVERLAY_ID = '__parcel__error__overlay__';
var OldModule = module.bundle.Module;

function Module(moduleName) {
  OldModule.call(this, moduleName);
  this.hot = {
    data: module.bundle.hotData,
    _acceptCallbacks: [],
    _disposeCallbacks: [],
    accept: function (fn) {
      this._acceptCallbacks.push(fn || function () {});
    },
    dispose: function (fn) {
      this._disposeCallbacks.push(fn);
    }
  };
  module.bundle.hotData = null;
}

module.bundle.Module = Module;
var checkedAssets, assetsToAccept;
var parent = module.bundle.parent;

if ((!parent || !parent.isParcelRequire) && typeof WebSocket !== 'undefined') {
  var hostname = "" || location.hostname;
  var protocol = location.protocol === 'https:' ? 'wss' : 'ws';
  var ws = new WebSocket(protocol + '://' + hostname + ':' + "61234" + '/');

  ws.onmessage = function (event) {
    checkedAssets = {};
    assetsToAccept = [];
    var data = JSON.parse(event.data);

    if (data.type === 'update') {
      var handled = false;
      data.assets.forEach(function (asset) {
        if (!asset.isNew) {
          var didAccept = hmrAcceptCheck(global.parcelRequire, asset.id);

          if (didAccept) {
            handled = true;
          }
        }
      }); // Enable HMR for CSS by default.

      handled = handled || data.assets.every(function (asset) {
        return asset.type === 'css' && asset.generated.js;
      });

      if (handled) {
        console.clear();
        data.assets.forEach(function (asset) {
          hmrApply(global.parcelRequire, asset);
        });
        assetsToAccept.forEach(function (v) {
          hmrAcceptRun(v[0], v[1]);
        });
      } else if (location.reload) {
        // `location` global exists in a web worker context but lacks `.reload()` function.
        location.reload();
      }
    }

    if (data.type === 'reload') {
      ws.close();

      ws.onclose = function () {
        location.reload();
      };
    }

    if (data.type === 'error-resolved') {
      console.log('[parcel] ✨ Error resolved');
      removeErrorOverlay();
    }

    if (data.type === 'error') {
      console.error('[parcel] 🚨  ' + data.error.message + '\n' + data.error.stack);
      removeErrorOverlay();
      var overlay = createErrorOverlay(data);
      document.body.appendChild(overlay);
    }
  };
}

function removeErrorOverlay() {
  var overlay = document.getElementById(OVERLAY_ID);

  if (overlay) {
    overlay.remove();
  }
}

function createErrorOverlay(data) {
  var overlay = document.createElement('div');
  overlay.id = OVERLAY_ID; // html encode message and stack trace

  var message = document.createElement('div');
  var stackTrace = document.createElement('pre');
  message.innerText = data.error.message;
  stackTrace.innerText = data.error.stack;
  overlay.innerHTML = '<div style="background: black; font-size: 16px; color: white; position: fixed; height: 100%; width: 100%; top: 0px; left: 0px; padding: 30px; opacity: 0.85; font-family: Menlo, Consolas, monospace; z-index: 9999;">' + '<span style="background: red; padding: 2px 4px; border-radius: 2px;">ERROR</span>' + '<span style="top: 2px; margin-left: 5px; position: relative;">🚨</span>' + '<div style="font-size: 18px; font-weight: bold; margin-top: 20px;">' + message.innerHTML + '</div>' + '<pre>' + stackTrace.innerHTML + '</pre>' + '</div>';
  return overlay;
}

function getParents(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return [];
  }

  var parents = [];
  var k, d, dep;

  for (k in modules) {
    for (d in modules[k][1]) {
      dep = modules[k][1][d];

      if (dep === id || Array.isArray(dep) && dep[dep.length - 1] === id) {
        parents.push(k);
      }
    }
  }

  if (bundle.parent) {
    parents = parents.concat(getParents(bundle.parent, id));
  }

  return parents;
}

function hmrApply(bundle, asset) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (modules[asset.id] || !bundle.parent) {
    var fn = new Function('require', 'module', 'exports', asset.generated.js);
    asset.isNew = !modules[asset.id];
    modules[asset.id] = [fn, asset.deps];
  } else if (bundle.parent) {
    hmrApply(bundle.parent, asset);
  }
}

function hmrAcceptCheck(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (!modules[id] && bundle.parent) {
    return hmrAcceptCheck(bundle.parent, id);
  }

  if (checkedAssets[id]) {
    return;
  }

  checkedAssets[id] = true;
  var cached = bundle.cache[id];
  assetsToAccept.push([bundle, id]);

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    return true;
  }

  return getParents(global.parcelRequire, id).some(function (id) {
    return hmrAcceptCheck(global.parcelRequire, id);
  });
}

function hmrAcceptRun(bundle, id) {
  var cached = bundle.cache[id];
  bundle.hotData = {};

  if (cached) {
    cached.hot.data = bundle.hotData;
  }

  if (cached && cached.hot && cached.hot._disposeCallbacks.length) {
    cached.hot._disposeCallbacks.forEach(function (cb) {
      cb(bundle.hotData);
    });
  }

  delete bundle.cache[id];
  bundle(id);
  cached = bundle.cache[id];

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    cached.hot._acceptCallbacks.forEach(function (cb) {
      cb();
    });

    return true;
  }
}
},{}]},{},["../../node_modules/parcel/src/builtins/hmr-runtime.js","index.js"], null)